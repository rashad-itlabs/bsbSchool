import 'package:flutter/material.dart';

/// Central palette so every feature shares one visual language.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF2E5BFF);
  static const Color primaryDark = Color(0xFF1B3FB8);
  static const Color accent = Color(0xFFFFA726);
  static const Color background = Color(0xFFF5F7FB);
  static const Color surface = Colors.white;

  // Dark theme
  static const Color backgroundDark = Color(0xFF121318);
  static const Color surfaceDark = Color(0xFF1E1F26);
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFC62828);
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
}
